export enum OrderOption {
  ASC = 'ASC',
  DESC = 'DESC',
}
export enum OrderByOption {
  CREATED_AT = 'createdAt',
  VIEWS = 'views',
}

export enum UserRole {
  ADMIN = 'admin',
  COMMON = 'common',
}
